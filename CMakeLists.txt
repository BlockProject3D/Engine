cmake_minimum_required(VERSION 3.10)
project(BlockProject)

add_subdirectory(Base)
add_subdirectory(Tests)
if (WIN32)
    add_subdirectory(RenderEngine.DX11)
    add_subdirectory(Tests.DX11)
endif (WIN32)
add_subdirectory(RenderEngine.GL40)
